CREATE TABLE Schedule_of_trains(
    Id INT NOT NULL AUTO_INCREMENT
    Number_of_train VARCHAR(50) NOT NULL
    Path_of_train VARCHAR(50) NOT NULL
    Type_of_train VARCHAR(50) NOT NULL
    Arrival_time VARCHAR(50)
    Departure_time VARCHAR(50) 
    PRIMARY KEY(Id)
)